function verificarTutela() {
    // Recuperar el objeto tutela del sessionStorage
    let tutela = JSON.parse(sessionStorage.getItem("cxm_tutela"));

    // Verificar si la propiedad tutela existe y es verdadera
    return tutela && tutela.tutela === true;
}
